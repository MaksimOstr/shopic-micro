spring:
  datasource:
    url: jdbc:postgresql://localhost:${CODE_DB_PORT}/${CODE_DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
logging:
  level:
    org:
      hibernate:
        SQL: debug